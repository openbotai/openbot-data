{
  "name": "@openbot/data-processor-infra",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest --run",
    "deploy:annotator": "wrangler deploy --config wrangler.annotator.jsonc",
    "deploy:processor": "wrangler deploy --config wrangler.processor.jsonc"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260719.1",
    "typescript": "^5.9.3",
    "vitest": "^4.1.0",
    "wrangler": "^4.112.0"
  }
}
