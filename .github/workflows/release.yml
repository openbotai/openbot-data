name: Release

on:
  release:
    types: [published]

jobs:
  test:
    name: Test Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.9", "3.10", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - run: git merge-base --is-ancestor "$GITHUB_SHA" origin/main
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - run: python -m pip install -e ".[dev,lerobot,hub]"
      - run: python scripts/check_version.py --tag "${GITHUB_REF_NAME}"
      - run: pytest -q
      - if: matrix.python-version == '3.9'
        run: ruff check openbot_data tests examples
      - if: matrix.python-version == '3.9'
        run: mypy openbot_data

  official-lerobot-conformance:
    name: Official LeRobot 0.6.0 conformance
    runs-on: ubuntu-latest
    env:
      OPENBOT_DATA_OFFICIAL_CONFORMANCE: "1"

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install FFmpeg
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg
      - run: python -m pip install -e ".[dev,lerobot,hub]"
      - run: python -m pip install "lerobot[dataset]==0.6.0"
      - run: pytest -q tests/test_official_lerobot_conformance.py

  build:
    name: Build distributions
    needs: [test, official-lerobot-conformance]
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Build distributions
        run: |
          python -m pip install --upgrade pip
          python -m pip install build twine
          python scripts/check_version.py --tag "${GITHUB_REF_NAME}"
          python -m build
          python -m twine check dist/*
          python -m venv /tmp/openbot-data-wheel
          /tmp/openbot-data-wheel/bin/pip install dist/*.whl
          /tmp/openbot-data-wheel/bin/python -c "import importlib.resources, openbot_data; assert importlib.resources.files('openbot_data').joinpath('py.typed').is_file()"

      - name: Upload distributions
        uses: actions/upload-artifact@v4
        with:
          name: python-package-distributions
          path: dist/
          if-no-files-found: error

  publish:
    name: Publish to PyPI
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/p/openbot-data
    permissions:
      id-token: write

    steps:
      - name: Download distributions
        uses: actions/download-artifact@v4
        with:
          name: python-package-distributions
          path: dist/

      - name: Publish distributions to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
